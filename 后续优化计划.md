# 后续优化计划

> 记于 2026-08-03，接着 `claude/known-issues-workflow-n2hkau` 往下做。
> 下次开工先读这份，再读 `交付文档.md` 的「三、这次改了什么」。

---

## 第一项：转化率看板 —— 同步改成按需触发 + 日/周/月日期筛选

### 1.1 只有日报才自动同步，其余改成按钮

**现状（这次刚改的，要部分回退）**：`/api/workbench/bootstrap` 无条件下发 `auto: true`，
`conversion.html` 的 `maybeAutoSync()` 一解析完就写多维表格 + 推异常。
周报/月报解析时也会触发，等于往群里刷不该发的东西。

**要改成**：
- 多维表格底账：**所有周期都照写**（基准线要靠全量分布，这个不动）
- 飞书群推送：**只有 `日报` 自动发**；周报/月报只在手动点按钮时发

**动手位置**：
- `app.py` 的 `bootstrap()`：`auto` 从布尔改成 `{"bitable": true, "webhook": ["日报"]}` 这种结构
- `conversion.html` 的 `maybeAutoSync()`：现在的守卫是
  `if(!$('#fsAuto').checked || !state.data) return;`
  改成分别判断两条路径 —— bitable 照旧，webhook 加一条
  `if(!AUTO_WEBHOOK_PERIODS.includes(state.data.period||PERIOD)) → 跳过并在状态里说明原因`
- 「推送异常到飞书」按钮（`#fsSend`）要提权成主操作：现在混在一堆 ghost 按钮里，
  周报/月报场景下它是唯一入口，得让人一眼看到（配合第三项一起做样式）

**注意**：去重集合 `autoDone.webhook` 的 key 是 `${period}|${tDate}`，
改完要确认手动点按钮那条路也走同一个 key，否则「自动跳过 + 手动点」会重复推送。

### 1.2 日/周/月都能筛日期做对比

**现状**：周期骨架已经有了，不用从零建 ——
- `PERIOD_ORDER = ['日报','周报','月报']`（约 658 行）
- `PERIOD_META`（654-656 行）已经定义好每个周期的文案：`cur/prev/dod/hist/minGap`
- 但周/月按钮（306-307 行）默认 `disabled`，靠 `avail[PERIOD].ok` 解锁，
  而 `avail` 是**从当前上传的这一个 Excel 里推出来的** —— 也就是说只能对比这份文件里恰好包含的两期

**要改成**：能选任意两期做对比，数据从多维表格底账读回来，而不是要求重新上传。

**动手位置**：
1. `app.py` 新增 `GET /api/feishu/bitable/records`：
   参数 `period` / `from` / `to`，转发飞书 `bitable/v1/apps/{app}/tables/{tbl}/records/search`
   （注意分页 `page_token`，和 `bitable_batch_create` 一样要循环取完）
   —— 底账里已经有 `日期/周期/来源/指标/所属环节/层级/本期值/上期值/环比/是否异常/角色` 全部字段，
   够画对比了
2. `conversion.html` 加一个「历史对比」面板：周期切换 + 两个日期选择器 + 对比表
   （`PANELS` 数组里加一项，`ALWAYS_ON` 里也要加 —— 它不依赖上传文件）
3. 空态要处理好：底账里没数据时说清楚「先跑几天日报积累底账」，别显示空白表

---

## 第二项：邮箱自动化 —— 干掉每天手动下载再上传

**现状痛点**：转化率和出单监控的报表都发在邮箱里，每天人工下载 → 再上传到工作台。

**目标**：工作日早 10 点开机后自动跑完，**然后问我要不要发群**（不是自动发 —— 和第一项是一件事）。

### 关键架构问题（必须先决策）

`conversion.html` 的解析逻辑**全在浏览器里跑**（客户端 JS）。要无人值守分析，三条路：

| 方案 | 做法 | 代价 |
|---|---|---|
| **A. 自动开页面（推荐）** | 后台取好附件 → 打开工作台 → 页面自动加载文件并解析 | 最省，和用户原话「自动打开工作台进行分析」一致；但机器得开着、浏览器得开 |
| B. Playwright 无头驱动 | 工作台内部用 Playwright 跑 conversion.html | 完全无人值守；但要在 Windows 上装 Playwright + Chromium |
| C. 逻辑移植到 Python | 把分类逻辑用 pandas 重写 | 最重，且和 HTML 版有行为漂移风险，不建议 |

**建议走 A**，理由是用户本来就是「早 10 点开机」的场景，人在机器前。
B 可以作为后续增强。

### 实施步骤（按 A）

1. **`mailbox.py`（新）** —— IMAP 收件
   - 用标准库 `imaplib` + `email`，不引第三方依赖
   - `config.json` 加 `mailbox` 段：`host / port / user / password(授权码) / folder /
     senders[] / subject_keywords[] / attachment_pattern`
   - 按「发件人 + 主题关键词 + 附件名正则」筛，取最近 N 天的附件
   - 落到 `data/inbox/<日期>/`，已处理的记 `.state.json` 防重复下载
   - ⚠️ 邮箱密码进 `config.json`（已 gitignore）。企业邮箱多半要用**授权码**不是登录密码

2. **定时触发** —— 复用 `feishu.py` 里那个后台线程模式，别引 APScheduler
   - 工作日 10:00 触发；`config.json` 配 `schedule: {"cron": "10:00", "weekdays": [1,2,3,4,5]}`
   - 开机自启：把 `启动工作台.vbs` 的快捷方式丢进
     `shell:startup`（Win+R 输入即可打开该目录）

3. **`GET /api/inbox/latest`** —— 把取到的文件交给前端
   - 返回 `{conversion: {...}, order_monitor: {...}}`，含文件名、日期、下载地址
   - `conversion.html` 启动时拉一次：有待处理文件就**自动加载并解析**，
     不用人去点「选择文件」

4. **出单监控自动跑** —— 这条已经通了，直接调现成的
   `POST /api/run/order-monitor`（附件路径换成 inbox 里的即可），
   `jobs.py` 的实时日志和 `_record` 流水都能复用

5. **「要不要发群」的确认入口**
   - 首页「今天要做的」变成待办：`转化率已解析 · 发现 3 个异常 [查看] [发飞书群]`
   - 点了才推 —— 正好落实第一项的「需要触发通知时才触发」
   - 可选：用现成的 `send_dm()` 给自己发条飞书私信提醒「今天的分析好了，去确认」

---

## 第三项：用 skill 美化两个工具看板

**好消息（实测）**：`conversion.html` 总共 724KB，但 **CSS 只有 19KB**，
2 个 `<script>`、**没有 base64 大块** —— 体积基本都是逻辑和文案，改样式的实际工作量不大。
`merchant.html` 989KB，预计结构类似，动手前先同样量一遍。

### 做法

1. **先加载 skill**：`artifact-design`（设计基本功）+ `dataviz`（这两个页面全是图表，必须读）
2. **复用已经定好的设计系统**，不要另起一套 —— 直接搬 `templates/index.html` 的 token：
   - 品牌蓝 `#1664ff` 只给主操作；语义色 `ok/warn/crit` 独立于品牌色
   - 灰阶偏冷（hue 220）
   - 所有数字/时间/百分比走等宽 + `font-variant-numeric: tabular-nums`
   - 浅色/深色双主题：token 定义在 `:root`，`@media (prefers-color-scheme)` 和
     `:root[data-theme=]` 各重定义一次，组件只走 token
3. **`conversion.html` 已经有主题切换器**（`crm_theme` localStorage，约 530 行），
   和看板的 `wb_theme` 统一成同一个 key，免得两个页面主题打架
4. **图表**按 `dataviz` 的规范重做：分类色板、区域填充、淡网格、强调端点、
   tooltip 和图例的一致性
5. `merchant.html` 的密码锁页（约 260 行）也一起做 —— 那是进工具的第一印象

### 顺序建议

先 `conversion.html`（天天用、且第一项要动它的 UI，合并一次改完），
再 `merchant.html`（只在出异常时才打开，优先级低）。

---

## 三项之间的依赖

```
第一项 1.1（同步改按需）──┐
                          ├─→ 一起做，都要动 conversion.html 的同步区 UI
第二项 步骤 5（确认入口）──┘

第一项 1.2（历史对比）──→ 依赖 /api/feishu/bitable/records（新接口）

第三项 ──→ 放在第一项之后做，避免刚做完样式又被功能改动推翻
```

**建议顺序**：第一项 1.1 → 第二项 → 第一项 1.2 → 第三项。

---

## 开工前要确认的事

1. 邮箱是什么服务（Exchange / 腾讯企业邮 / Gmail），IMAP 开了没，有没有授权码
2. 报表邮件的发件人、主题、附件命名规律 —— 筛选规则要靠这个写
3. 周报/月报除了「不自动推群」，要不要也**不自动写多维表格**（目前计划是照写）
4. 第二项走 A（开机后自动开页面）还是 B（Playwright 完全无人值守）
