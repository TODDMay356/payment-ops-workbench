# 交接说明

> 记于 2026-08-07。**新会话接手先读这份**，再按需翻下面点名的那几份。
> 主干是 `main`，工作台的内容就在仓库根目录（以前在 `workbench/` 子目录下）。

> **仓库在 08-07 重组过，已完成。** 原来一个仓（叫 `googthing`）里塞了 6 个互不
> 相干的项目、8 个自动命名的 `claude/*` 分支、没有 `main`，默认分支还指着一个
> 没人动的收银台演示，而且是 public 的。
>
> 现在这个仓叫 **`payment-ops-workbench`**，private，只放工作台一个项目，
> `main` 是主干（`workbench/` 用 `git subtree split` 提升到了根，26 个真正
> 碰过它的提交，历史保留）。搬走的三个各自独立成仓，历史也都保留：
>
> | 原来在哪 | 现在在哪 |
> |---|---|
> | 根目录 `index*.html`（收银台交互演示） | `TODDMay356/checkout-demo` |
> | `faq-tools/`（带 Dockerfile/Jenkinsfile 的 RAG 服务） | `TODDMay356/faq-tools` |
> | `daily_report_sync/` | `TODDMay356/daily-report-sync` |
>
> **`claude/feasibility-discussion-xbnszw` 这个分支是故意留着的**，别删：
> 它上面的 `merchant_analysis.html` 有一个 `trendCompareCard`（本期 vs 上期
> 双线趋势图，实线/虚线），现行 `static/merchant.html` 里没有对应实现 ——
> `trendCard` 整个函数里 `compare` 一次都没出现，而文件其它地方用了 68 次，
> **界面上那个「对比上期」开关还在**。要么把它补回来，要么确认不需要再删分支。

---

## 一、先去哪儿找什么

| 想知道 | 看哪份 |
|---|---|
| 怎么装、怎么配、日常怎么用 | `README.md` |
| 还剩什么没做 | `后续优化计划.md` 末尾「剩余待办」 |
| 改完怎么验证 | `tests/README.md` |
| 某处代码**为什么**这么写 | `git log` —— commit message 里写了理由，比代码注释全 |
| 早期几轮改了什么 | `交付文档.md`（**只覆盖到第三轮，之后没再更新**，以本文件为准） |

---

## 二、这个东西是干什么的

本机跑的 Flask 小服务（`127.0.0.1:5050`），把两个单文件 HTML 工具 + 飞书 + Outlook 串起来：

```
Outlook 邮件 ──mailbox.py──> data/inbox/<kind>/<业务日期>.xlsx
                                    │
              ┌─────────────────────┴─────────────────────┐
              ▼                                           ▼
      转化率（浏览器里解析）                       出单监控（子进程跑脚本）
      首页出待办卡片，点了才开                     首页出按钮，点了才跑
              │                                           │
              ▼                                           ▼
      点按钮才发飞书 / 写多维表格              群通知 + BD 私聊（脚本自己发）
```

**两个工具页的分析逻辑全在浏览器 JS 里**，服务端只做中转、取信、起进程。

---

## 三、不要「顺手改回去」的决定

这些是有理由的，代码上看不出来，改之前先读理由：

**1. 解析完不自动发飞书、也不自动写多维表格。**
两条都只有点按钮才发。用户明确要过。（多维表格那条原计划是保留自动写的，
实现时一起砍了 —— 用户后来确认「不用恢复」。）

**2. 出单监控取到报表也不自动跑。**
`mailbox.json` 里 `order_monitor.auto_run` 默认 `false`。
取信是省事，自动往群里发消息是另一回事。用户原话：「没必要，取到了让我自己手动触发就可以」。

**3. 业务日期从邮件主题里抠，抠不出来就报错，绝不退回「当天」。**
旧版就是靠退回当天，把昨天的报表当成今天跑了，「新审核通过」判定、开通天数、
周报星期判断全错。`mailbox.match()` 抠不出来直接抛 `MailboxError`。

**4. 商户密码的哈希在两个文件里各存一份**（`merchant.html` 的 `ACCESS_SHA256`、
`templates/index.html` 的 `MA_SHA256`）。不合并是因为 `merchant.html` 要能脱离工作台
单文件离线打开。**改密码要两处一起改**，两边都有注释指向对方。

**5. 每个工具一个常驻 iframe，回首页也不清 `src`。**
以前共用一个 iframe 且 `goHome()` 置 `about:blank`，切一趟商户回来解析结果就没了。
代价是内存：**实测一份 4.6MB 报表解析完标签页占约 380MB**，两个工具都装满按两份算。
「重新加载本工具」按钮是释放阀。

**6. 品牌色是蓝 `#1664ff`，不是绿。**
工具页原来 `--brand:#1f7a4d` 和 `--good:#0ca30c` 都是绿的 —— 这两个页面全靠语义色
传达严重度，品牌色和「好」撞成一族会读错。三个界面现在同一支蓝，语义色独立。

**7. 「重点关注商户」卡片的严重度分档是「比例 + 绝对值封顶」，不是纯阈值。**
见 `conversion.html` 的 `sevOf()`。纯绝对阈值试过，08-02 那天最大 4.2% 好用，
换 08-03（最大 31%）就六张里五张标红，等于没改。纯相对也不行 —— 平静日最大 0.1%
也会标红。所以按占当天最大值的比例分档，再用绝对值封顶。

**8. 表格影响列按「整表是拉升还是拖累」着色，不按每行数字的正负号。**
摩擦类指标（3DS交易占比之类）跌了才是好事，那种行的「影响比率」是负数却是正向贡献。
下钻本来就按方向筛过行了，所以整表同色才对。

**9. `--t` 必须写成 `transition:<属性> var(--t)`，不能写 `transition:var(--t)`。**
这个 token 只有时长和缓动（`140ms cubic-bezier(...)`），没有属性名 ——
写成 `transition:var(--t)` 会让 `transition-property` 落回 `all`。
看着更短，实际是让浏览器盯住每个属性，还会动到没打算动的东西。
`merchant.html` 里 `.switch` 原来的 `transition:.15s` 是同一个毛病，已一并改掉。

**10. 通栏整行的控件故意没有按压缩放。**
`.hbar`（商户）、`.sys-bar`（首页）、所有 `details>summary` 行 —— 整条横贯的元素
按下去缩放很怪。它们只给背景色反馈。skill 里对应的是 `static` 那条例外。

**11. 商户页影响力图是「红=提高、绿=拖累」，不是蓝红。**
用户明确要的红涨绿跌那套读法。`--lift:var(--critical)`、`--drag:var(--good)`。
**这和同页语义色是撞着的** —— `--critical` 红此刻同时表示「严重/差」
（成功率低的 KPI 数字就是红的）和「提高」；`--good` 绿同时是「好」和「拖累」，
两个意思相隔不到两厘米。知道这点，仍然照用户要的做，别顺手换回蓝红。
真觉得读着别扭时的备选：给影响力图单独一对不同明度的红绿，和语义色拉开距离。

---

## 四、这轮（08-04 ~ 08-07）做了什么

按时间倒序，7 个 commit：

| commit | 做了什么 |
|---|---|
| `081d8d6` | 把 `make-interfaces-feel-better` skill 收进 `.claude/skills/` |
| `ea8178a` | 三个界面统一美化：绿换蓝、卡片严重度分档、量级条、阈值条收起 |
| `d07420a` | 常驻 iframe（切走不丢数据）、商户密码提到首页 |
| `caa6f39` | 修 `el.hidden` 对 flex 元素完全无效（`[hidden]{display:none!important}`） |
| `5327135` | 缺同目录 `.py` 时说人话，不甩 traceback |
| `42dc5ad` | probe 在没建 `mailbox.json` 时也能用（以前必然报 0 匹配） |
| `146ef4e` | probe 从一堆 `\uXXXX` 改成能读的页面 |
| `77def98` | 出单监控改成点按钮触发，不再取到就跑 |
| `dc419bd` | 邮箱自动化：`mailbox.py` + `automation.py` |
| `854863a` | 播报从大环节穿透到真正在动的子/孙环节 |

**新增的文件**：`mailbox.py`（取信存档）、`automation.py`（定时 + 编排）、
`mailbox.example.json`、`tests/`、`.claude/skills/`。

---

## 五、还没做的

1. **周月报的 AI 提示词**。注意计划文档里「缺页面入口」那句**已经过期** ——
   `#aiBtn` 按钮在「播报素材」面板底部，`buildAIPrompt()` 也已经带周期了。
   真正欠的是：现在喂给 AI 的是**按日报巡检口径写的播报素材**，
   周报/月报需要一套做趋势和商户维度分析的提示词。

2. **被白名单静默丢掉的「来源」行**。`conversion.html:611` 的
   `ALLOWED_SOURCES = ['独立站API直连','独立站标准收银台']` 是精确匹配，
   三处 filter（约 776/785/806 行）直接把其它来源无声丢弃 ——
   报表里出现新来源时用户不会知道。

3. ~~用 `make-interfaces-feel-better` 审一遍三个界面。~~ **已做**，见第八节。

4. **chip 删除按钮的热区只到 24×24**，没到 skill 要求的密集桌面 40×40。
   `merchant.html` 的 `.chips{gap:8px}` 左右各只能吃 4px，再大就和相邻 chip 的热区重叠。
   要到 40 得先把 gap 提到 16px —— 那是布局改动，当时没做。

---

## 六、容易踩的坑

- **改了模板一定要重启**。`config.json` 里 `debug:false`，Flask 把模板缓存在内存里。
  用 `启动工作台.vbs`（它每次先关旧进程）。判断新版生效：打开 `/api/inbox/probe`
- **用户是手动复制文件更新的，不是 `git pull`**。只复制 `app.py` 会因为
  `import automation` 起不来 —— 已经改成会明说缺哪个文件，但**同目录的 `.py` 是一整套**
- **`mailbox.json` ≠ `mailbox.py`**。前者是配置（自己建、有密码、gitignore），
  后者是代码。名字只差扩展名，实际认错过
- **Windows 的「新版 Outlook」不提供 COM 接口**，必须是经典桌面版。
  走不通时 `provider` 改 `folder` 兜底
- **别用 `pkill -f "app.py"`** 停服务，会把跑命令的 shell 自己杀掉
- 用户机器上的 Outlook 报表在 `lanmei@inflyway.com/收件箱/商户运营关注邮件`

---

## 七、当前状态

用户侧已确认「一切正常」：邮箱自动化跑通、交互修复和美化都已拉取生效。
四套用例在容器里全过。分支已推送，**没有开 PR**。

---

## 八、交互打磨（08-07，按 `make-interfaces-feel-better`）

上一轮（`ea8178a`）做的是**信息设计**——严重度分档、语义色、量级条。
这一轮补的是**交互层**，两者不重叠，skill 自己也这么分册。三个界面全做了。

| 原则 | 改了什么 |
|---|---|
| 按压反馈 | 三个文件 `:active` 原本**一处都没有**。给 13 类控件加了 `scale:.96`（skill 规定死这个值，小于 .95 就夸张了），走 CSS transition 所以可中断、松手能弹回 |
| 具名 transition | 四处 `transition:var(--t)` + 两处 `transition:.15s` 实际都是 `transition:all`，全部改成具名属性。见第三节第 9 条 |
| reduced-motion | `merchant.html` 是唯一没有这条媒体查询的，而它恰好有个无限转的 `.spin`。补齐，三个界面现在一致 |
| 热区 | `.chip button` 16×16 → 伪元素撑到 24×24（受 gap 限制，见第五节第 4 条）；两个工具页的 `.theme button` 约 25px 高 → 纵向撑到 40px |
| hover 过渡 | `.tm` / `.nav-tabs button` / `.chip button` / `.hbar` / `.sys-bar` 原来是直接跳变，补 120ms |
| 同心圆角 | `.tabs` 12→13px（内 `.tab` 8px + padding 5px）、`.tm` 11→13px。`.seg` 和 `.theme` 本来就算对了，没动 |
| text-wrap | 原本只有 `.head h1` 一处。标题加 `balance`，描述/说明类短文本加 `pretty` |

**明确没做**（下次别当成遗漏补上）：

- `.card` / `.kpi` / `.tm` 的 `border + box-shadow` **没有**换成 skill 的分层 `--shadow-border`。
  用户选的是「不动视觉」；而且 `.tm` 的 `border-left:3px solid var(--sev)` 是**严重度状态通道**，
  skill 原则 3 自己就说状态性的 border 要留着
- `.up-cell` 的同心圆角没算。外 14px、padding 16px，公式反推内圆角要 ≤0，
  规则在这个量级失效（skill 也说 padding 接近 24px 时按独立表面处理）
- 没加 `will-change`。skill：只在**观察到**首帧卡顿时加
- 没加入场 stagger。这些面板每次改筛选都重渲染，属于高频交互，skill 原则 5 明确不要

### 补记：`ea8178a` 留下的四个悬空变量（08-07 修复）

`ea8178a` 换配色时，把 `merchant.html` 三个主题块里这两行的**定义**删了，
**引用一处没删**：

```
--lift:#2a78d6; --drag:#d03b3b; --mid:#eceef0;
--series:#2a78d6; --series-soft:#cde2fb;
```

`fill:var(--lift)` 里变量未定义 → 整条声明失效 → SVG `fill` 回落到初始值**黑色**。
表现是影响力图整排条和图例色块全黑，以及「成功率·维度」图里
凡是 ≥整体、走 `var(--series)` 的柱子也全黑（低于整体的走 `--serious`/`--critical`，
反而是好的，所以看起来像「只有一根有颜色」）。

修法不是把三个主题块补回去，而是在浅色 `:root` 里写一次、引用现有语义 token：
自定义属性在**使用时**才求值，深色块覆盖 `--brand`/`--critical`/`--good` 之后会自动跟随。

**留了一条扫描**，以后改配色可以先跑一遍找悬空引用：
比对文件里 `var(--x)` 的集合和 `--x:` 定义的集合，差集应为空。
当时三个文件里只有 `merchant.html` 有问题（4 个），另外两个是干净的。

**验证到哪一步**：容器里用 Playwright 无头 Chromium 实按了按钮，读到 `:active`
期间计算值 `scale=0.96`、松手回 1；伪元素热区量到 24×24 和 42×40；
`reduced-motion` 下 `.spin` 的 `animation-name` 是 `none`、过渡时长 0s；
首页、转化率浅色深色都截图看过，无版式回归。
**`tests/` 那四套用例没跑** —— 依赖 `$WB_FIXTURES` 里的真实报表，仓库里没有。
本轮是纯 CSS 改动，但拉取后**建议照常跑一遍**，尤其 `e2e4.py`。
