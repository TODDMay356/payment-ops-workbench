# 跨境支付运营工作台 — 交付文档

> 更新时间：2026-08-03
> 工作台地址：`http://127.0.0.1:5050`（本机 Flask 服务）

---

## 一、功能概览

| 工具 | 类型 | 飞书同步 | 说明 |
|---|---|---|---|
| 每日转化率数据监控 | 内嵌 iframe（HTML） | ✅ webhook + bitable | 上传 Excel → 解析异常 → 推群 + 写多维表格，**凭据由工作台下发，解析后自动同步** |
| 商户转化率&成功率分析 | 内嵌 iframe（HTML） | ❌ 纯分析 | 单商户漏斗 / 失败原因 / AI 归因，有密码锁 |
| 出单耗时监控 | 后台 Python 任务 | ✅ 群通知 + BD 私聊 | 上传两个 Excel → 对比分类 → **一次运行自动发群和私聊**，日志实时显示在首页 |

---

## 二、文件清单

```
D:\work\doing\workbench\
├── app.py                Flask 主程序，所有路由
├── feishu.py             飞书客户端（token / webhook / bitable / DM）
├── jobs.py               后台任务运行器（进程 + 实时日志流）
├── config.json           实际配置（含凭据，已 gitignore）
├── config.example.json   配置模板（只有占位符）
├── requirements.txt      flask + requests
├── start.bat / start_hidden.vbs / install.bat
├── README.md
├── templates/index.html  工作台首页（当天动作流看板）
├── scripts/
│   └── 出单监控_workbench.py  ← 复制到下面那个目录，覆盖旧的同名文件
├── static/
│   ├── conversion.html   每日转化率工具
│   └── merchant.html     商户成功率工具
└── venv/

<order_monitor.dir，默认 D:\商户数据\出单耗时监控\出单监控\>
├── 出单监控.py             不用动
├── 出单监控_BD私聊版.py     不用动
└── 出单监控_workbench.py   ← 用 workbench/scripts/ 下的新版覆盖
```

---

## 三、这次改了什么

### 已修复

| # | 原描述 | 真实原因 | 处理 |
|---|---|---|---|
| 1 | 出单监控 CMD 窗口弹窗不稳定 | `CREATE_NEW_CONSOLE` 起完进程立刻 `return {"ok":true}`，**脚本成没成功工作台根本不知道**，弹窗只是表象 | 新增 `jobs.py`：后台起进程（Windows 用 `CREATE_NO_WINDOW`，不弹窗），stdout 逐行读取，经 SSE 实时推到页面，结束报退出码 |
| 2 | 转化率 HTML 的 localStorage 需先点"一键配置" | **键名不匹配**：首页写 `fsAuto`/`fsCardMode`，工具页读 `auto`/`cardMode` → 自动同步开关从来没被勾上，`maybeAutoSync()` 第一行守卫就返回 | 新增 `/api/workbench/bootstrap`，工具页启动时自取配置、自动勾选、锁定输入框。"一键配置并打开"按钮已删除 |
| 3 | tenant_access_token 2h 过期，需确认后台线程在跑 | `_refresh_loop` 一次失败后照样再睡 90 分钟，一次网络抖动就可能撞上 2h 过期，且无从得知 | 失败改为退避重试（30s→60s→120s→300s）；`/api/status` 暴露 `thread_alive` / `last_refresh_at` / `last_error`，首页状态栏直接显示 |
| 5 | 出单监控 BD 私聊版未集成 | 旧 wrapper 走「出单监控.py」的 `main()`，那条路只发群通知；BD 私聊要另开脚本再跑一遍，等于把两份 Excel 重算两次 | 「出单监控_BD私聊版.py」的 `run_pipeline()` 本来就是超集，新 wrapper 直接复用它的函数：一次分析 → 群通知 + 多维表格 + BD 私聊。`--mode both` 为默认，可改 `group` / `dm` |

### 出单监控这次额外修的

- **业务日期用错**：`run_pipeline()` 靠 `extract_date_from_filename()` 从文件名抠 `YYYY-MM-DD`，
  而工作台把上传文件存成 `today.xlsx` / `yesterday.xlsx`，抠不出来就退回 `datetime.now()`。
  跑昨天的报表会被当成今天 —— 实测同一份数据，业务日期给 `2026-08-02` 时某商户是
  「🆕 新审核通过-待出单」，退回到运行当天就变成「👀 3天内未出单」，分类、BD 私聊内容全跟着错。
  新 wrapper 改成显式传 `--date`
- **AppSecret 硬编码在脚本里**：你这次重置密钥后，脚本里那份就失效了。改成工作台经环境变量下发，
  以后换密钥只改 `config.json` 一处
- **结尾的 `input("按回车键关闭窗口...")`**：没有控制台后 stdin 是 DEVNULL，`input()` 抛 EOFError，
  会把一次成功的运行显示成失败。已去掉
- **本地 txt 存到临时上传目录**：那个目录没人会去看，改存脚本目录
- **模块顶层 `import tkinter`**：精简版 Python 会直接 ImportError 把任务打死。没有 tk 时用空壳顶上
- 出单监控脚本直连飞书、不走工作台中转，所以首页流水收不到。加了任务完成回调补记，
  失败时把 stdout 最后一行的真实原因带上（如「密码不正确」），而不是只给一个退出码

### 顺带修掉的（原文档没记，但确实是 bug）

- 侧边栏点「出单监控」调 `runOrderMonitor()` 不带参数 → `e.preventDefault()` 抛异常，点了没反应
- `abort(400, ...)` 返回 HTML，前端 `r.json()` 解析失败 → 所有参数校验错误都显示成假的「网络错误」
- `FeishuClient.get_token()` 持 `threading.Lock` 再调 `_refresh_token_blocking()`（后者也要拿同一把锁）→ 只要走到"token 缺失/过期"这条路就**永久死锁**。已改 `RLock`
- `bitable_batch_create` 不分批，超过飞书 500 条上限整批失败，报错还和字段类型错误长得一样。已按 500 分批
- CSS 引用了从未定义的 `--gray-400`
- `templates/index.html` 把**真实密码**写在 input 的 placeholder 里
- `static/` 下有一份和 `merchant.html` 逐字节相同的 1MB 备份副本，已删

### 未做

| # | 项 | 原因 |
|---|---|---|
| 4 | 商户成功率加飞书同步 | 明确不做，保持纯分析工具 |
| 6 | 飞书智能伙伴待办同步 | 待定。需要先确定用哪个接口（飞书任务 Task API / aily 智能伙伴） |

### ⚠️ 需要你自己处理的安全问题

`config.example.json` 之前带着**真实的** `app_secret` 和 personal webhook URL 提交进了公开仓库，
`templates/index.html` 的 placeholder 里也写了真实密码。本次交付的模板已清空，
但**已进 git 历史，删文件无效** —— 必须去飞书开放平台重置 AppSecret、重建那个自定义机器人。

---

## 四、日常使用流程（6 步 → 3 步）

1. 双击桌面快捷方式（启动 + 开浏览器）
2. 首页「今天要做的」：
   - 转化率：点"打开并上传 Excel" → 选文件 → 自动同步
   - 出单监控：两份报表 + 日期 + 密码 → "开始运行" → 页面里看日志
3. 「今天同步了什么」确认结果；有异常再进商户成功率深挖

**取消掉的步骤：**
- 原第 2 步「确认首页 5 项全绿」—— 状态栏全绿时收成一行，只在出问题时才需要看
- 原第 3 步「点一键配置并打开」—— 凭据由工作台下发，按钮已删
- 原第 4 步「看弹窗输出」—— 不弹窗了，日志在页面里

---

## 五、架构要点

```
FeishuClient（模块级单例）
├── token：启动拉取 → 后台 90 分钟续期 → 失败退避重试 → 状态可查（thread_alive / last_error）
├── webhook：HMAC-SHA256 签名 → broadcast_webhook(body, targets=None) 同 URL 去重、可按 team/personal 路由
├── bitable：文本→飞书类型转换（日期毫秒戳 / 百分比小数 / 是否异常布尔）→ 500 条分批写入
└── DM：App 身份发私信（可选）

jobs.py（后台任务）
├── start_job() → Popen（管道 stdout、CREATE_NO_WINDOW、PYTHONUNBUFFERED=1）
├── 读取线程 → deque(5000 行) + Event 唤醒
└── stream() → 阻塞生成器 → app.py 包成 SSE

前端（当天动作流看板）
├── 56px 图标轨（原 220px 侧边栏）
├── 状态栏：全绿一行；异常自动展开成 6 格明细
├── 今天要做的：转化率入口 + 出单监控就地表单 & 实时日志
└── 今天同步了什么：/api/activity 流水，红/绿区分成败
```

设计 token：品牌蓝 `#1664ff` 只用在主操作和激活轨；语义色 `ok/warn/crit` 独立于品牌色；
灰阶统一偏冷（hue 220）；所有数字、时间、状态走等宽 + `tabular-nums`；浅色/深色双主题。

---

## 六、Bitable 字段

| 字段名 | 飞书类型 | 转换 |
|---|---|---|
| 日期 | 短日期 | `"2026-07-31"` → 毫秒时间戳 |
| 周期 | 单选 | `日报` `周报` `月报` |
| 来源 / 指标 / 所属环节 | 文本 | — |
| 层级 | 单选 | `大环节` `子环节` `孙环节` |
| 本期值 / 上期值 / 环比 | 百分比 | `"82.35%"` → `0.8235` |
| 是否异常 | 复选框 | `"是"` → `true` |
| 角色 | 单选 | `根因` `未解释` `局部` `已解释` |
